
Generate a new Prisma migration (database schema change) and apply it to the database:
npx prisma migrate dev --name init


Add a field e.g `direction` to the `Message` model in the Prisma schema:
npx prisma migrate dev --name add-message-direction


Run website:
npm run dev

Run websocket:
npx tsx src/lib/websocket-server.ts

Connect to rpi:
ssh pi@raspberrypi

Run the sms listener on rpi (tmux session recommended):
tmux

cd ~/Desktop/tina/rpi_sms
./start.sh

To exit tmux session:
Ctrl + b, then d

#const mode = settings.navigationMode as NavigationMode
